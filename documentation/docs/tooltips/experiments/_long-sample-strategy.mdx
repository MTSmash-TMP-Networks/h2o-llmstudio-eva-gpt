Select how training samples longer than the tokenizer max length are handled when starting an experiment. `Truncate long samples` keeps the previous behavior, `Use sliding window` splits long samples into overlapping chunks, and `Skip long samples` leaves them out of the training dataset.
