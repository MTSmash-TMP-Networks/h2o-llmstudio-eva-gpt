Number of tokens that consecutive sliding-window chunks share when the long sample strategy is set to `Use sliding window`. A larger overlap preserves more context between chunks but creates more training samples.
